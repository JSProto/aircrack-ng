

define(['parser/lib/namespace', 'parser/lib/parser', 'parser/lib/stream'], function(NS, Parser, Stream) {

    return {
        NameSpace : NS,
        Parser : Parser,
        Stream : Stream
    }
})